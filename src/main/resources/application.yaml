spring:
  application:
    name: travel-planner-agent

embabel:
  agent:
    platform:
      llm-operations:
        data-binding:
          max-attempts: 5
    logging:
      personality: default
      verbosity: info
